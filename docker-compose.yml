services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env

  scheduler:
    build: .
    command: python scheduler.py
    env_file:
      - .env

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend